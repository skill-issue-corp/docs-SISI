# FAQ по устранению неполадок

## Общее

### `System.DllNotFoundException` на ARM (например, M1) Mac

В настоящее время мы не поддерживаем запуск SS14/Robust нативно на ARM Mac. Установите x64 .NET и запускайте клиент с ним, это должно работать через эмуляцию Rosetta 2.

## Клиент

## Сервер

### Ошибки привязки портов при попытке запустить сервер.

Некоторые выписанные коды ошибок, которые у вас могут быть, для поиска ctrl+f: "An attempt was made to access a socket in a way forbidden by its access permissions.", "Only one usage of each socket address (protocol/network address/port) is normally permitted."

По умолчанию игровому серверу для запуска нужны порты 1212/UDP и 1212/TCP. Частая причина здесь - *уже запущенный сервер SS14, использующий эти порты*.

#### Пользователи Windows

Установка **Docker for Windows** или чего-то связанного, похоже, полностью ломает диапазон динамических портов по умолчанию в вашей системе, и вдобавок резервирует кучу портов через постоянно работающий сервис. Это означает, что *даже когда Docker не запущен*, он или *любое приложение в вашей системе* может случайно решить зарегистрировать порт 1212 для общих целей. 

Если вы хотите убедиться, что это так, запустите `netsh int ipv4 show dynamicportrange tcp`. По умолчанию это *должно* быть следующим:

```
Protocol tcp Dynamic Port Range
---------------------------------
Start Port      : 49152
Number of Ports : 16384
```

Если нижняя граница `Start Port` равна примерно 1024, ваша система, насколько я могу судить, сломана. Это можно исправить, выполнив `netsh int ipv4 set dynamicportrange tcp startport=49152 numberofports=16384` в командной строке с повышенными правами, чтобы вернуть диапазон динамических портов к рекомендациям IANA, а затем перезагрузившись.
